const config = {// Veritabanı bağlantısı burada kurulur.
    db: {
        host: "localhost",
        user: "admin",
        password: "admin",
        database: "proje"
    },
    
    PORT: 3000,
};

module.exports = config;